[package]
name = "bimodal-array"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Dual-mode access to a contiguous array."
readme = "README.md"
repository = "https://github.com/afgTheCat/bimodal-array"

[dependencies]

[dev-dependencies]
static_assertions = "1.1.0"
